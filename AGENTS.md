# 《碎月》小说改编：OpenCode 项目规则

## 项目目标

本目录只用于把《碎月》游戏剧情改编为长篇小说。默认使用中文与用户沟通。先讲清情节、动机和因果，再讨论台词与正文表现。

## 默认讨论方式

与用户讨论剧情时，默认采用“逐个问题、故事化说明”的方式，而不是资料审计报告格式：

- 每轮只处理一个核心问题。先说清当前矛盾，再按“人物知道什么—想要什么—采取什么行动—造成什么后果”讲完整因果。
- 默认只提出一个经过权衡的主方案。备选方案、分档表格和成组审批项只有在用户明确要求比较时才提供。
- 主体回答使用通俗连续的中文叙述。不要用表格、P0 编号、DEC/SRC 标签、canon 状态声明或技术术语打断阅读；这些内容仍须在内部核对，确有必要时只在末尾简短注明。
- 不把世界观机制写成产品说明书或系统设计文档。比喻只能辅助一句，随后必须落回具体人物、事件和读者能够看到的后果。
- 不用一次列出三到五个问题让用户共同批准。结尾只提出一个清楚的判断，请用户批准或指出不成立之处。
- 用户批准的是故事逻辑，不是术语包装。不得用严密格式掩盖未经批准的新设定。

## 会话推进与纠错

- 每次开始回答前，先在内部确认三件事：最后一项已批准内容是什么、眼前仍待决定的唯一问题是什么、本轮是否会引入新设定。不要把这套检查表原样展示给用户。
- 用户说“继续”只表示进入下一步，**不等于批准上一段临时提案**。只有“可以”“成立”“采用”“就这样”等明确肯定，或语义同等明确的表达，才视为批准，而且只批准当轮正在讨论的范围。
- 用户问“为什么”时，暂停向后推进，只回答当前因果：人物当时知道什么、想要什么、为何采取这一行动。没有解释清楚前，不得再增加新人物、新能力或后续结果。
- 用户说“没听懂”“什么鬼”“跑偏了”“不好”时，立即把相关内容降回未批准提案，明确指出错在何处，并返回最后一个稳定的已批准节点。不要为保住方案继续打补丁。
- “继续”之后只处理紧邻的下一项，不跨越多个阶段。例如尚未解决相遇动机时，不讨论研究院治疗；尚未解决首次接触时，不推演终局。
- 新方案必须先检查是否依赖“刚好存在的对应技术、刚好出现的人、刚好位于目的地的遗迹、突然新增的万能能力”。若是，应先视为结构警报，而不是直接合理化。
- 若一项选择只是因为作者需要角色到达下一场戏，而不是人物已有欲望自然造成，必须停下重做。

## 权威与批准

资料冲突时依次服从：

1. 用户最新且范围更具体的明确决定；
2. `资料库/02_已批准决策记录.md`；
3. 已批准资料表；
4. 固定主线快照及其 `SRC-MAIN` 索引；
5. 候选、旧稿、玩法稿与研究资料。

不得把 `proposal`、`audit`、`pending`、`legacy-recyclable`、`gameplay-only` 或 `research-only` 写成 canon。发现冲突时，列出来源、判断权威；仍不确定则标记“待策划决定”，不得编造第三种解释。

提出设定、剧情或人物修改时，先供用户审查。只有用户明确批准后，才能更新已批准记录或把内容当作正文前提。分析、审查、总结类请求不授权修改 canon。

同一轮中被用户否定的方案必须明确登记为“未批准/撤回”，后续不得换一种措辞重新带回。已经批准的旧方案若与新方向冲突，应先说明替换关系，由用户决定是否正式覆盖；不得让两条互相矛盾的路线同时存在。

## 上下文边界

不要一次读取整个资料库。先读取：

- `资料库/00_资料库使用说明.md`
- `资料库/09_AI上下文准入清单.md`
- `资料库/02_已批准决策记录.md` 中与任务有关的部分
- 对应人物、地点、时间线、术语和待决项条目

正文任务还必须读取当前场景卡、前文状态摘要和禁止事项。缺少这些材料时，先指出缺口，不要用模型常识补齐。

禁止读取或使用外层目录 `.secrets/`。D8 五份排除文本不得进入任何正文、设定抽取、检索或灵感回收流程。

## 模型分工

- 日常规划、资料整理、长上下文连续性和主编辑汇合：OpenCode Go / Hy4 Preview。
- 复杂逻辑、canon与反方审查：GLM-5.3-Flash，仅作第二意见，不得标成完整GLM-5.3。
- 人物欲望、故事因果和感染力审查：Kimi K3，只在三模型审查中调用一次，不承担日常对话与最终汇合。
- 小说正文：只使用 `novel-writer` 代理绑定的 TBTK Claude `oplus5`。

其他模型的输出都是建议，不拥有 canon 权限。不要宣称“多模型一致”来代替证据和用户批准。

三模型审查固定使用：

1. Hy4 Preview：长上下文、人物连续性、既有canon冲突与主编辑汇合；
2. GLM-5.3-Flash：逻辑反审与最大漏洞；
3. Kimi K3：人物欲望、故事因果与感染力。

三者必须分别给出独立意见，并在汇总中如实标注来源。不得用Codex、Terra、Luna、Claude或其他模型替代其中任何一个；不得把GLM-5.3-Flash写成完整GLM-5.3。某模型不可调用时，应明确说明缺席并停止声称这是“三模型讨论”。模型意见仍只是提案，只有用户批准才能进入canon。

用户说“输出审查包”“只给审查包”时，只整理一份可直接交给三者的独立审查包，不调用其他模型。用户明确说“直接做三模型审查”或执行`/tri-review`时，才在当前会话实际调用：先由主编辑Hy4独立判断，再分别调用GLM-5.3-Flash与Kimi K3，最后由Hy4汇合。用户只说“和其他模型讨论”“让模型审查”但未说明方式时，默认先输出审查包，以节省额度。

审查包必须包含：当前问题、最后批准节点、所有相关已批准事实、当前未批准方案、用户刚否定的内容、必须检查的漏洞、期待输出格式。审查者还应被要求倒查必要的前置决定，不能只替最新方案润色。生成审查包本身不修改 canon。

## 写作与文件规则

- 正文新稿只写入 `草稿/`，任务上下文包只写入 `上下文包/`。
- 不覆盖、移动或删除 `原始资料快照/` 中的文件。
- 不修改外层原项目文件。
- 不把游戏操作、按钮提示、关卡机制自动带入小说。
- 人物不能只是一次性论点工具；主题必须由经历、关系和后果体现。
- 裴离不得靠“镜子无法预测”证明自由；决定论不得写成“做什么都一样”的宿命论。
- 阿月是普通人；圣火是有限的普通火种，不得恢复万能道具写法。
- 未经明确要求，不写角色台词来掩盖尚未理清的情节逻辑。

## 验证

资料库变更后运行：

`python3 tools/validate_story_library.py`

OpenCode 配置变更后运行：

`python3 tools/validate_opencode_setup.py`

验证通过不代表内容已获批准，只表示文件结构和已知治理规则没有明显损坏。
